<div class="container">
    {{> header}}
    <div class="details-box">
        {{!-- <span class="titlebar">{{height}}</span>
        <span class="titlebar">{{width}}|</span> --}}
        {{>frameDetailsForm}}
        
        {{!--
        <div>
            {{#each members}} {{> teamMember}} {{else}}
            <p>No member info</p>
            {{/each}}
        </div> --}} {{!--
        <span class="spanner">Description</span>
        {{#if comment}}
        <p>{{comment}}</p>
        {{else}}
        <p>No description</p>
        {{/if}} --}}
        <span class="spanner">Team management</span>
        {{> frameControls}}
    </div>
</div>